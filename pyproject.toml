[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "gymnasium_mars_lander"
version = "0.0.1"
authors = [
    { name = "Quentin Deschamps", email = "quentindeschamps18@gmail.com" },
]
description = "A reinforcement learning environment for the Mars Lander CG puzzle"
readme = "README.md"
requires-python = ">=3.10"
keywords = ["Reinforcement Learning", "game", "RL", "AI", "gymnasium", "pygame"]
license = { text = "MIT" }
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Games/Entertainment",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "gymnasium==1.2.3",
    "pygame==2.6.1",
]

[project.optional-dependencies]
training = [
    "matplotlib==3.10.9",
    "moviepy==2.2.1",
    "rl-zoo3==2.8.0",
    "tensorboard==2.20.0",
]
testing = ["pytest"]
quality = ["black[d]", "isort", "pylint"]

[project.urls]
Repository = "https://github.com/Quentin18/gymnasium-mars-lander"

[tool.black]
line-length = 88

[tool.isort]
profile = "black"

[tool.pep8]
max-line-length = "88"

[tool.pylint."MESSAGES CONTROL"]
max-line-length = 88
disable = [
    "attribute-defined-outside-init",
    "duplicate-code",
    "inconsistent-return-statements",
    "invalid-name",
    "missing-function-docstring",
    "missing-module-docstring",
    "too-many-arguments",
    "too-many-instance-attributes",
    "too-many-positional-arguments",
]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra"
